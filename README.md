# Strata, Vercel-ready

Import this folder into Vercel as a static project. Leave Build Command and Output Directory empty.

The hero loads one source only: optimized H.264 mobile video on phones, original video on desktop. The supplied poster is painted behind the video and is preserved on load, decode, or autoplay failure, preventing a black hero.
